apiVersion: v1
kind: Route
metadata:
  name: route-dev-webapp
spec:
  host: route-dev-webapp-gitops-user1.apps.cluster-6h459.dynamic.redhatworkshops.io
  path: /
  to:
    kind: Service
    name: dev-webapp
    weight: 100
  port:
    targetPort: 80
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
  
