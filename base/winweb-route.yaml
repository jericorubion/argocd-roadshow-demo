apiVersion: v1
kind: Route
metadata:
  name: webapp
spec:
  path: /
  to:
    kind: Service
    name: webapp
    weight: 100
  port:
    targetPort: 80
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
  
